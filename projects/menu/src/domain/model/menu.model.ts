export interface IMenuResponse {
  id: number;
  menuName: string;
  description: string;
  active: boolean;
}

export interface IMenu {
  menuName: string;
  description: string;
  active: boolean;
}
